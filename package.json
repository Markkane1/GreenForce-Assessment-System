{
  "name": "epa-testing-suite",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "server": "npm --prefix server run server",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
